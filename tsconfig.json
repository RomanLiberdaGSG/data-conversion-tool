{
  "compilerOptions": {
    "target": "es5",
    "module": "commonjs",
    "outDir": "./dist", // Output directory for compiled JavaScript files
    "strict": true
  },
  "include": ["src/**/*.ts"], // Include all TypeScript files in the src directory and its subdirectories
  "exclude": ["node_modules"] // Exclude the node_modules directory
}
